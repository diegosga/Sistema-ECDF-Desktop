{
  "name": "cecom-electron",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "dev": "concurrently \"vite\" \"wait-on http://localhost:5173 && electron .\"",
    "start": "electron .",
    "build:frontend": "cd cecom-sistema && npm install && npm run build",
    "build:backend": "cd cecom-back-end && npm install && npx prisma migrate deploy && npx prisma generate && npm run build && npm prune --production",
    "prebuild": "npm run build:frontend && npm run build:backend",
    "build": "electron-builder"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "electron": "^42.2.0",
    "electron-builder": "^26.8.1"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "electron-store": "^11.0.2",
    "xlsx": "^0.18.5"
  }
}
